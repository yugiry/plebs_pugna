using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// ChatGPT-4 2023/5/8
// 任意の場所にオブジェクトを配置（座標フリー）
public class Tile_test : MonoBehaviour
{
    

    //int i;
    //int j;

    public GameObject circlePrefab;
    //private int count=0;
    //public void ClickDown()
    //{
    //    if (Input.GetMouseButtonDown(0)&&count==0)
    //    {
    //        Vector3 position = Camera.main.ScreenToWorldPoint(Input.mousePosition);
    //        //position.z = 0f;
    //        circlePrefab.transform.position = new Vector3(i, j, 0);
    //        for (i = -80; i < 40; i+=40)//tate
    //        {
    //            for (j = -80; j < 40; j+=40)//yoko
    //            {
    //                //GameObject newCircle = Instantiate(circlePrefab/*, position, Quaternion.identity*/);
    //                GameObject newCircle = Instantiate(circlePrefab, circlePrefab.transform.position, Quaternion.identity);
    //            }
    //        }
    //        // Do something with newCircle here, if needed
    //        count++;
    //    }


    //}

    void Update()
    {

    //    if (Input.GetMouseButtonDown(0)&&count==0)
    //    {
    //        //Vector3 position = Camera.main.ScreenToWorldPoint(Input.mousePosition);
    //        //position.z = 0f;

    //        //GameObject newCircle = Instantiate(circlePrefab, position, Quaternion.identity);
    //        // Do something with newCircle here, if needed

    //        ClickDown();

    //    }
    //    if (Input.GetMouseButtonDown(0) && count == 1)
    //    {
    //        Destroy(this.gameObject);
    //        count--;
    //    }
       }
}
